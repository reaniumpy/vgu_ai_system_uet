# Core dependency only: the prompt-injection detection model (the "engine").
# The UI, serving, and I/O stack is intentionally left open for the fresh,
# Docker-based rebuild -- add whatever the new design actually needs.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.8.0
transformers==4.57.6
