# ── Detection engine (the "guard") ───────────────────────────────────────────
# Off-the-shelf prompt-injection classifier (DeBERTa-v3) run locally on CPU.
# Kept in a separate file so the Docker model layer stays cached across app changes.
-r requirements-engine.txt

# ── Web app (UI + serving + file I/O) ─────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20          # file uploads (multipart/form-data)
itsdangerous==2.2.0               # signed session cookies (role sign-in)
pypdf==6.14.2                     # extract text from uploaded PDFs
python-docx==1.1.2                # extract text from uploaded Word docs

# ── Downstream "AI assistant" (only runs on SAFE documents) ───────────────────
# OpenAI / ChatGPT. Only the stable chat.completions surface is used, so a range
# keeps the image build robust while still pulling a modern SDK.
openai>=1.40,<2.0
